#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@wkocjan/gatsby-theme-intro/node_modules/react-dev-utils/node_modules/browserslist/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../@wkocjan/gatsby-theme-intro/node_modules/react-dev-utils/node_modules/browserslist/cli.js" "$@"
  ret=$?
fi
exit $ret
