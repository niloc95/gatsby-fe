import React from "react"
import { number, string } from "prop-types"
import LibfrSvg from "./libfra.svg"

const Libfr = ({ libfr, i }) => (
  <li
    className={`relative flex justify-center items-center rounded-full border-2 border-skill-${i}`}
  >
    <span className="absolute font-header font-semibold text-front text-sm md:text-base px-2 text-center">
      {libfr}
    </span>
    <LibfrSvg className={`w-full text-skill-${i} opacity-75 m-2`} />
  </li>
)

Libfr.propTypes = {
  libfr: string.isRequired,
  i: number.isRequired,
}

export default Libfr
